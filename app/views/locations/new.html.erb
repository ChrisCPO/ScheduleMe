<h1> Add New Location </h1>

<%= form_for @location do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit "create location" %>
  </div>
<% end %>
